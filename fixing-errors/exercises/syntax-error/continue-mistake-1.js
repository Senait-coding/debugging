'use strict';

/*
  environment:

  name:
  message:

  callstack:

  life cycle:

  the mistake: the "continue" was added uncessarly

  the fix(es):delete the "countinue"
*/

const userName = 'wingdoz';

if (userName.length > 3) {
  console.log(userName);

}
