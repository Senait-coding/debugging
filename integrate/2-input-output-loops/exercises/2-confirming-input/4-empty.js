'use strict';
let userInput='';
let userConfirm=true;

while(userConfirm){userInput=prompt('enter your favorite color')

const userConfirmed=userInput + "  " + 'is your favorite color?'
userConfirm=alert(userConfirmed)}

const finalMessage = `your favorite color  is: "${userInput}"`;
alert(finalMessage);